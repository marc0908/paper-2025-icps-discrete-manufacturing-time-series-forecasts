"""Core project modules: training, common utilities, and config wrappers.

These modules re-export from legacy locations to keep backward compatibility
while we transition to the structured layout.
"""